CREATE TABLE PRODUTO (ID  SERIAL NOT NULL, ESTOQUE INTEGER, NOME VARCHAR(255), PRECO FLOAT, PRIMARY KEY (ID))
CREATE TABLE PESSOA (ID  SERIAL NOT NULL, DTYPE VARCHAR(31), DTCADASTRO DATE, NOME VARCHAR(255), SEXO VARCHAR(255), NOTA FLOAT, SALARIO FLOAT, PRIMARY KEY (ID))
