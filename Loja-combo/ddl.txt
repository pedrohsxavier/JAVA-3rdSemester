CREATE TABLE PRODUTO (ID  SERIAL NOT NULL, ESTOQUE INTEGER, NOME VARCHAR(255), PESO FLOAT, PRECO FLOAT, PRATELEIRA_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE PRATELEIRA (ID  SERIAL NOT NULL, PESOMAXIMO FLOAT, PRIMARY KEY (ID))
ALTER TABLE PRODUTO ADD CONSTRAINT FK_PRODUTO_PRATELEIRA_ID FOREIGN KEY (PRATELEIRA_ID) REFERENCES PRATELEIRA (ID)
